import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
  container: {
    width: '100%',
  },
  btnsGroup: { height: 50 },
  bgImg: {
    width: '100%',
    height: '100%',
  },
  badgeStyleContainer: { position: 'absolute', top: -10, right: -40 },
  badgeStyle: { height: 30, width: 30 },
});

export default styles;
